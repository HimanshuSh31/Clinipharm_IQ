# .env.example — copy to .env and fill in your values
# Load automatically with: pip install python-dotenv
# Then add this to main.py:  from dotenv import load_dotenv; load_dotenv()

# Admin credentials
PHARMACY_ADMIN_USER=admin
PHARMACY_ADMIN_PASS=change_me_before_production
